generator client {
  provider = "prisma-client"
  output   = "./generated/data"
}

datasource db {
  provider = "sqlite"
}

model Word {
  id              Int              @id @default(autoincrement())
  external_id     Int?
  value           String?
  nsl             String?
  isv             String?
  transcription   String?
  field           String?
  declension      Int?
  etymology       String?
  genesis         String?
  type            String?
  pos             String?
  frequency       String?
  intelligibility String?
  addition        String?
  sameInLanguages String?

  proto           String?          // Праславянская исходная форма: *synъ, *ryba, *světъ
  paradigm        String?          // Акцентная парадигма Зализняка: A, B, C
  protoStemClass  String?          // Класс праславянской основы: ā, jā, o, jo, i, u, consonant
  stemExtension   String?          // Наращение основы: en, es, et, er

  // Обратные связи Prisma
  meanings        Meaning[]
  roots_words     RootWord[]
  synonymsRoot    Synonym[]        @relation("SynonymRoot")
  synonymsWord    Synonym[]        @relation("SynonymWord")
  antonymsRoot    Antonym[]        @relation("AntonymRoot")
  antonymsWord    Antonym[]        @relation("AntonymWord")

  @@map("words")
}

model Meaning {
  id       Int       @id @default(autoincrement())
  wordId   Int
  meaning  String?
  examples String?

  // Связь с таблицей слов
  word     Word      @relation(fields: [wordId], references: [id], onDelete: Cascade)

  // Обратные связи для языковых таблиц
  en_word  En[]      @relation("EnWord")
  en_mean  En[]      @relation("EnMeaning")
  ru_word  Ru[]      @relation("RuWord")
  ru_mean  Ru[]      @relation("RuMeaning")
  mk_word  Mk[]      @relation("MkWord")
  mk_mean  Mk[]      @relation("MkMeaning")
  sr_word  Sr[]      @relation("SrWord")
  sr_mean  Sr[]      @relation("SrMeaning")
  uk_word  Uk[]      @relation("UkWord")
  uk_mean  Uk[]      @relation("UkMeaning")
  bg_word  Bg[]      @relation("BgWord")
  bg_mean  Bg[]      @relation("BgMeaning")
  pl_word  Pl[]      @relation("PlWord")
  pl_mean  Pl[]      @relation("PlMeaning")
  be_word  Be[]      @relation("BeWord")
  be_mean  Be[]      @relation("BeMeaning")
  cs_word  Cs[]      @relation("CsWord")
  cs_mean  Cs[]      @relation("CsMeaning")
  sk_word  Sk[]      @relation("SkWord")
  sk_mean  Sk[]      @relation("SkMeaning")
  sl_word  Sl[]      @relation("SlWord")
  sl_mean  Sl[]      @relation("SlMeaning")
  hr_word  Hr[]      @relation("HrWord")
  hr_mean  Hr[]      @relation("HrMeaning")
  cu_word  Cu[]      @relation("CuWord")
  cu_mean  Cu[]      @relation("CuMeaning")
  de_word  De[]      @relation("DeWord")
  de_mean  De[]      @relation("DeMeaning")
  nl_word  Nl[]      @relation("NlWord")
  nl_mean  Nl[]      @relation("NlMeaning")
  eo_word  Eo[]      @relation("EoWord")
  eo_mean  Eo[]      @relation("EoMeaning")

  @@map("meanings")
}

model Root {
  id    Int     @id @default(autoincrement())
  value String?
  type  Int?    @default(0)

  roots_words RootWord[]

  @@map("roots")
}

model RootWord {
  id     Int  @id @default(autoincrement())
  wordId Int?
  rootId Int?

  word   Word? @relation(fields: [wordId], references: [id], onDelete: Cascade)
  root   Root? @relation(fields: [rootId], references: [id], onDelete: Cascade)

  @@map("roots_words")
}

model Synonym {
  id        Int    @id @default(autoincrement())
  rootId    Int?
  wordId    Int?
  proximity Float?

  root      Word?  @relation("SynonymRoot", fields: [rootId], references: [id], onDelete: Cascade)
  word      Word?  @relation("SynonymWord", fields: [wordId], references: [id], onDelete: Cascade)

  @@map("synonims")
}

model Antonym {
  id        Int    @id @default(autoincrement())
  rootId    Int?
  wordId    Int?
  proximity Float?

  root      Word?  @relation("AntonymRoot", fields: [rootId], references: [id], onDelete: Cascade)
  word      Word?  @relation("AntonymWord", fields: [wordId], references: [id], onDelete: Cascade)

  @@map("antonims")
}

// Динамические таблицы языков, преобразованные в схемы Prisma
model En {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("EnWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("EnMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("en")
}

model Ru {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("RuWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("RuMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("ru")
}

model Mk {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("MkWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("MkMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("mk")
}

model Sr {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("SrWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("SrMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("sr")
}

model Uk {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("UkWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("UkMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("uk")
}

model Bg {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("BgWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("BgMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("bg")
}

model Pl {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("PlWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("PlMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("pl")
}

model Be {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("BeWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("BeMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("be")
}

model Cs {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("CsWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("CsMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("cs")
}

model Sk {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("SkWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("SkMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("sk")
}

model Sl {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("SlWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("SlMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("sl")
}

model Hr {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("HrWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("HrMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("hr")
}

model Cu {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("CuWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("CuMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("cu")
}

model De {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("DeWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("DeMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("de")
}

model Nl {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("NlWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("NlMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("nl")
}

model Eo {
  id        Int      @id @default(autoincrement())
  value     String?
  veryfied  Int?
  wordId    Int?
  meaningId Int?

  word      Meaning? @relation("EoWord", fields: [wordId], references: [id], onDelete: Cascade)
  meaning   Meaning? @relation("EoMeaning", fields: [meaningId], references: [id], onDelete: Cascade)

  @@map("eo")
}
